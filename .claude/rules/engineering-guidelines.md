# Engineering Guidelines

Follow the project rules and conventions in the repo root `CLAUDE.md`.

Universal conventions (HTTP/API, observability, testing, error codes) are **not** copied into `docs/` — reference the `code-conventions` skill at runtime. `docs/` holds project-private convention documents only; add an index entry in `CLAUDE.md` when one is added.
