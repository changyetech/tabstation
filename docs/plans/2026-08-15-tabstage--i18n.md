# tabstage 子计划：多语言（--i18n）

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 自定义轻量 i18n（spec §7）：语言解析纯函数 + en/zh_CN 字典 + `useT()` hook，不引第三方库。

**Architecture:** `resolveLanguage` 纯函数决定生效语言；`I18nProvider` 注入 Context；`useT()` 返回 `t(key, params)`。字典是扁平 key 的 JSON。**本文件的字典是全项目 UI 文案的唯一登记处**——后续 UI 子计划只消费这里定义的 key，新增文案必须回到这两个 JSON 补条目。

**Tech Stack:** React Context + JSON 字典 + Vitest/Testing Library。

**Depends on:** `--scaffold`。

## Global Constraints

- 不引第三方 i18n 库；语言标签 `zh-CN`（禁用 zh-Hans），字典文件名 `zh_CN.json`
- 注释用简体中文；先写失败测试

---

### Task 1: 语言解析 `resolveLanguage`

**Files:**
- Create: `src/i18n/resolve.ts`
- Test: `src/i18n/resolve.test.ts`

**Interfaces:**
- Produces: `type Language = 'en' | 'zh-CN'`；`resolveLanguage(setting: 'auto' | Language, navigatorLanguage: string): Language` —— Task 2 与 `--ui-list` 的 App 消费

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it } from 'vitest';
import { resolveLanguage } from './resolve';

describe('resolveLanguage', () => {
  it('显式设置直接生效', () => {
    expect(resolveLanguage('en', 'zh-CN')).toBe('en');
    expect(resolveLanguage('zh-CN', 'en-US')).toBe('zh-CN');
  });
  it('auto：navigator 是 zh-* 时取 zh-CN（V1 仅两种语言，zh 变体统一简体）', () => {
    expect(resolveLanguage('auto', 'zh-CN')).toBe('zh-CN');
    expect(resolveLanguage('auto', 'zh')).toBe('zh-CN');
    expect(resolveLanguage('auto', 'zh-TW')).toBe('zh-CN');
  });
  it('auto：其余取 en', () => {
    expect(resolveLanguage('auto', 'en-US')).toBe('en');
    expect(resolveLanguage('auto', 'ja')).toBe('en');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm vitest run src/i18n/resolve.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```ts
export type Language = 'en' | 'zh-CN';

// 语言解析（spec §7）：auto 跟随 navigator.language；zh-* 统一落到 zh-CN（V1 仅 en/zh-CN 两种）
export function resolveLanguage(
  setting: 'auto' | Language,
  navigatorLanguage: string
): Language {
  if (setting !== 'auto') return setting;
  return navigatorLanguage.toLowerCase().startsWith('zh') ? 'zh-CN' : 'en';
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm vitest run src/i18n/resolve.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
rtk git add src/i18n/resolve.ts src/i18n/resolve.test.ts
rtk git commit -m "feat(i18n): 语言解析（auto 跟随浏览器）"
```

---

### Task 2: 字典 + Provider + `useT()`

**Files:**
- Create: `src/i18n/en.json`, `src/i18n/zh_CN.json`, `src/i18n/index.tsx`
- Test: `src/i18n/index.test.tsx`

**Interfaces:**
- Consumes: `Language`（Task 1）
- Produces:
  - `<I18nProvider language={Language}>`
  - `useT(): (key: string, params?: Record<string, string | number>) => string`（缺 key 时返回 key 本身）
  - `useLanguage(): Language`
  - 全部 UI 子计划消费；字典 key 全集如下，UI 计划中出现的 `t('…')` 均以此为准

- [ ] **Step 1: 创建字典（全项目文案唯一登记处）**

`src/i18n/en.json`：

```json
{
  "toolbar.modeWindow": "By window",
  "toolbar.modeAll": "All tabs",
  "toolbar.viewList": "List",
  "toolbar.viewDomain": "Domain",
  "toolbar.dedupe": "Dedupe",
  "toolbar.history": "History",
  "toolbar.settings": "Settings",
  "window.label": "Window {n}",
  "window.current": " (current)",
  "window.tabCount": "{n} tabs",
  "window.save": "Save window",
  "window.close": "Close window",
  "window.closeConfirm": "Close {name} and its {n} tabs?",
  "tab.activate": "Go to tab",
  "tab.close": "Close",
  "tab.readLater": "Read later",
  "tab.moveTo": "Move to",
  "move.newWindowMaximized": "New window (maximized)",
  "move.newWindowSameSize": "New window (same size)",
  "readLater.title": "Read later",
  "readLater.delete": "Delete",
  "sessions.title": "Saved sessions",
  "sessions.open": "Open",
  "sessions.delete": "Delete",
  "sessions.rename": "Rename",
  "sessions.emptySnapshot": "No tabs to save",
  "settings.title": "Settings",
  "settings.managerPageScope": "Manager page singleton",
  "settings.scopeGlobal": "One for the whole browser",
  "settings.scopePerWindow": "One per window",
  "settings.closeWindowAfterSave": "Close window after saving a session",
  "settings.language": "Language",
  "settings.langAuto": "Auto (follow browser)",
  "settings.langEn": "English",
  "settings.langZh": "简体中文",
  "domain.localFiles": "Local files",
  "domain.other": "Other",
  "time.justNow": "just now",
  "dup.badge": "×{n}"
}
```

`src/i18n/zh_CN.json`：

```json
{
  "toolbar.modeWindow": "窗口模式",
  "toolbar.modeAll": "全部模式",
  "toolbar.viewList": "列表",
  "toolbar.viewDomain": "域名",
  "toolbar.dedupe": "一键去重",
  "toolbar.history": "历史",
  "toolbar.settings": "设置",
  "window.label": "窗口 {n}",
  "window.current": "（当前窗口）",
  "window.tabCount": "{n} 个 tab",
  "window.save": "保存窗口",
  "window.close": "关闭窗口",
  "window.closeConfirm": "关闭{name}及其 {n} 个 tab？",
  "tab.activate": "跳转",
  "tab.close": "关闭",
  "tab.readLater": "稍后阅读",
  "tab.moveTo": "移动到",
  "move.newWindowMaximized": "新窗口（全屏）",
  "move.newWindowSameSize": "新窗口（同尺寸）",
  "readLater.title": "稍后阅读",
  "readLater.delete": "删除",
  "sessions.title": "已保存会话",
  "sessions.open": "打开",
  "sessions.delete": "删除",
  "sessions.rename": "重命名",
  "sessions.emptySnapshot": "没有可保存的 tab",
  "settings.title": "设置",
  "settings.managerPageScope": "管理页单例",
  "settings.scopeGlobal": "全浏览器一个",
  "settings.scopePerWindow": "每窗口一个",
  "settings.closeWindowAfterSave": "保存会话后关闭窗口",
  "settings.language": "语言",
  "settings.langAuto": "自动（跟随浏览器）",
  "settings.langEn": "English",
  "settings.langZh": "简体中文",
  "domain.localFiles": "本地文件",
  "domain.other": "其他",
  "time.justNow": "刚刚",
  "dup.badge": "×{n}"
}
```

- [ ] **Step 2: 写失败测试**

`src/i18n/index.test.tsx`：

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { I18nProvider, useT } from './index';

function Probe({ id, params }: { id: string; params?: Record<string, string | number> }) {
  const t = useT();
  return <span data-testid="out">{t(id, params)}</span>;
}

describe('useT', () => {
  it('按语言取文案', () => {
    render(
      <I18nProvider language="zh-CN">
        <Probe id="toolbar.dedupe" />
      </I18nProvider>
    );
    expect(screen.getByTestId('out')).toHaveTextContent('一键去重');
  });

  it('参数插值', () => {
    render(
      <I18nProvider language="en">
        <Probe id="window.tabCount" params={{ n: 5 }} />
      </I18nProvider>
    );
    expect(screen.getByTestId('out')).toHaveTextContent('5 tabs');
  });

  it('缺 key 返回 key 本身', () => {
    render(
      <I18nProvider language="en">
        <Probe id="no.such.key" />
      </I18nProvider>
    );
    expect(screen.getByTestId('out')).toHaveTextContent('no.such.key');
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `pnpm vitest run src/i18n/index.test.tsx`
Expected: FAIL（模块不存在）

- [ ] **Step 4: 实现 `src/i18n/index.tsx`**

```tsx
import { createContext, useCallback, useContext, type ReactNode } from 'react';
import en from './en.json';
import zhCN from './zh_CN.json';
import type { Language } from './resolve';

export type { Language } from './resolve';
export { resolveLanguage } from './resolve';

const dictionaries: Record<Language, Record<string, string>> = {
  en,
  'zh-CN': zhCN,
};

const I18nContext = createContext<Language>('en');

export function I18nProvider({ language, children }: { language: Language; children: ReactNode }) {
  return <I18nContext.Provider value={language}>{children}</I18nContext.Provider>;
}

export function useLanguage(): Language {
  return useContext(I18nContext);
}

// t(key, params)：{name} 形式插值；缺 key 返回 key 本身（便于发现漏译）
export function useT() {
  const lang = useContext(I18nContext);
  return useCallback(
    (key: string, params?: Record<string, string | number>): string => {
      let text = dictionaries[lang][key] ?? key;
      if (params) {
        for (const [k, v] of Object.entries(params)) {
          text = text.replaceAll(`{${k}}`, String(v));
        }
      }
      return text;
    },
    [lang]
  );
}
```

- [ ] **Step 5: 运行确认通过**

Run: `pnpm vitest run src/i18n/index.test.tsx`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
rtk git add src/i18n
rtk git commit -m "feat(i18n): en/zh_CN 字典 + I18nProvider + useT"
```
