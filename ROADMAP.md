# ROADMAP

V1 之外「想要但延后」的方向。主动否决的非目标（搜索、内嵌历史面板、新标签页接管等）见设计规格的非目标章节，不在此列。

## 性能

- **标签行渲染跳过（`React.memo`）**：`useTabs` 每次刷新都产出全新的 tab 对象，行组件必然全量重渲染。要真正跳过，需要按字段比较的 memo + 全部回调稳定化，且移动菜单的目标列表要改成「打开时才算」（否则被跳过的行会拿到陈旧的窗口列表，是功能回归）。2026-08-17 优化 pass 已压掉刷新频率与每行的多项式成本，此项收益递减、风险偏高，故延后。（来源：docs/specs/2026-08-17-perf-stability-pass.md）
- **大会话恢复懒加载**：恢复 N 个 tab 的会话时全量加载，内存/网络冲击大。探索创建后 `tabs.discard` 或按需激活方案（discard 时序脆弱，需等 tab 进入 loading 后执行）。（来源：2026-08-15 设计评审 Q13）

## 功能

- **关闭操作撤销栈**：一键去重 / 关闭窗口误操作后可撤销。V1 以确认框 + hover 预览代替。（Q8）
- **Chrome Tab Group 支持**：展示组名/颜色、移动不脱组、会话保存并还原分组；需新增 `tabGroups` 权限。（Q4）
- **AI 批量操作（观察项，不排期）**：文本入口的低频批量指令（如语义分组会话）。前置条件：Chrome 内置本地模型（Gemini Nano / Prompt API）成熟到无需外发 URL/标题，不破坏纯本地零网络请求的隐私叙事；接云端 API 的形态直接否决。语音形态已否决，见设计规格非目标章节。（来源：2026-08-15 AI/语音价值分析）
