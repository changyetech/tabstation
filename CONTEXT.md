# tabstation

Chrome MV3 扩展的 TAB 管理上下文：一个集中管理页操作真实浏览器 tab，附带稍后阅读与窗口会话两类本地持久化数据。

## Language

### 页面与展示

**自有页面（Own Page）**:
Tab Station 自身的页面——管理页、设置页、新标签页。判定依据唯一：URL 以扩展根前缀（`chrome.runtime.getURL('')`）开头。自有页面在列表、计数、去重统计与会话快照中一律不可见，且不被关闭类操作波及（关闭其所在窗口时只关其余 tab）。
_Avoid_: 内部页、扩展页

**管理页（Manager Page）**:
Tab Station 的常驻集中管理页面，按作用域保持单例；点击扩展图标或按快捷键聚焦它。它是一种自有页面。
_Avoid_: 管理器、面板、dashboard

**新标签页（New Tab Page）**:
接管 Chrome 原生新标签页后呈现的页面，渲染与管理页完全相同的界面。它是一种自有页面，但**不是**管理页：它临时、可同时存在多个、不参与管理页单例查找。
_Avoid_: 主页、起始页

**设置页（Settings Page）**:
独立于管理页的偏好设置页面，经扩展「选项」入口或管理页页脚入口打开；所有改动即时保存并生效，无确认步骤。
_Avoid_: 设置对话框、偏好面板

**主题（Theme）**:
界面外观偏好，三选一：浅色 / 深色 / 自动（跟随系统）。管理页与设置页共用同一偏好。
_Avoid_: 皮肤、夜间模式

**模式（Mode）**:
主区的展示维度，三选一：全部模式（跨窗口按域名聚合）/ 窗口模式（按窗口分区，行按真实 index）/ 已保存会话（会话卡片列表）。三者按 全部 → 窗口 → 已保存会话 首尾相接构成固定循环序，任一模式的前后相邻模式恒定。
_Avoid_: 布局、视图

**默认视图（Default View）**:
管理页与新标签页打开时进入的模式，二选一：全部模式 / 窗口模式（已保存会话不可作默认）。页面内手动切段后以手动选择为准，直至页面关闭。
_Avoid_: 首页模式、初始布局

### 持久化实体

**稍后阅读（Read Later）**:
待读 URL 清单。保存即关闭源 tab，打开即从清单移除；条目也可不打开直接删除。
_Avoid_: 书签、收藏

**会话（Saved Session）**:
某窗口 tab 的模板式快照，可反复恢复而不消耗；条目可排序、单条删除、单条打开，删空即会话消亡。
_Avoid_: 备份、书签组

### 规则概念

**URL 归一化（URL Normalization）**:
全项目唯一的 URL 等价定义（去掉 hash）。去重统计与稍后阅读判重共用同一实现。

**重复组（Duplicate Group）**:
归一化后 URL 相同、数量 ≥ 2 且并非全部为固定标签的 tab 集合，统计范围恒为全浏览器所有窗口。

**固定标签（Pinned Tab）**:
Chrome 原生 pinned tab。列表中带标记、不可拖拽、不可收进稍后阅读；去重时优先于普通 tab 被保留。

**关闭动效（Close Effect）**:
关闭类动作的统一反馈：swoosh 音效 + 纸屑粒子 + 元素退场。批量关闭时音效只播一次、粒子逐行错开。

**展示条数（Visible Tab Count）**:
单个区块默认展示的 tab 行数上限（5 / 8 / 12 / 全部），超出部分折叠，区块底部以「展开 / 收起」切换。

**新窗口尺寸策略（New Window Size Policy）**:
一切「在新窗口打开」类动作创建窗口时的尺寸选择，二选一：全屏 / 随当前窗口同尺寸。

**拆到新窗口（Split to New Window）**:
把单个 tab 或某域名的全部 tab 从原窗口移出、放入按新窗口尺寸策略新建的窗口。固定标签不参与。
